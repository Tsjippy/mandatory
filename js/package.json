{
  "name": "mandatory",
  "version": "1.0.0",
  "description": "mandatory",
  "main": "./mandatory.js",
  "scripts": {
    "mandatory": "webpack --entry ./mandatory.js --output-path . --output-filename mandatory.min.js --progress --mode=production --devtool source-map",
    "admin": "webpack --entry ./admin.js --output-path . --output-filename admin.min.js --progress --mode=production --devtool source-map",
    "all": "run-p mandatory admin"
  },
  "author": "Ewald Harmsen",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.111.0",
    "webpack-cli": "^7.2.3"
  }
}
